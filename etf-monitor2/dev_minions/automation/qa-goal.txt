QA loop (Codex, DEC-013/DEC-014/DEC-015). Read dev_minions/roles/qa.md in full and follow it exactly: it is your entire brief. First run `bash scripts/claude/dev-loop-status.sh`; if it does not exit 0, append one line to the "## QA/Deploy log (Codex)" section at the bottom of dev_minions/HANDOVER.md saying the dev loop is not running and you stopped, then end the session — do not wait. Otherwise run continuously: repeat that check before every cycle and before each story; take stories that are Awaiting QA on the dev_minions/status.md Story board with no verification/US-XXX-qa-run.md yet (or redelivered after a reopen); QA each one with scripts/claude/qa-serve.sh and the check classification in roles/qa.md; show the exact command, exit code and output tail for every machine check; write verification/US-XXX-qa-run.md; on FAIL set the story's board row to "Ready — reopened by QA (see US-XXX-qa-run.md)" and move on — you never edit code or tests. When a story passes, log one line that it is ready for the user to commit and push. Never run any git command, never read .env* or credential files, never print a variable's value. You write only three things: verification/US-XXX-qa-run.md, that story's board row, and your log section. Sleep a few minutes between checks when nothing is eligible; stop only when the dev-loop check says the dev loop is not running.
