# US-012 review

## Round 1 — 2026-09-24

Verdict: PASS

Reviewer: `story-reviewer` (independent context; did not write this code). Sources read: `AGENTS.md`, `dev_minions/backlog/stories/US-012.md`, `dev_minions/verification/US-012-plan.md`, `dev_minions/decisions/DEC-010-us003-schema-and-driver.md`, `dev_minions/roles/technical-lead.md` (review checklist), `lib/db/schema.ts`, `lib/db/index.ts`, `lib/extraction/adapters/{types,validate,registry}.ts`, `lib/extraction/discovery.ts`, and every file under "Files changed" in `dev_minions/HANDOVER.md`, in full: `lib/ingestion/{select-values,store,ingest-etf,default-deps}.ts` and their `.test.ts`/`.pglite.test.ts` counterparts, `lib/ingestion/boundaries.test.ts`, `test/helpers/pglite.ts`, `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`. Ran `pnpm typecheck` (clean) and `pnpm lint` (0 errors, 1 pre-existing unrelated warning in `lib/extraction/adapters/types.test.ts`, already logged non-blocking in a prior round). Did not run `pnpm test`/`pnpm build` (out of my permitted scope; that is `story-tester`'s gate) and did not run any git command.

Scope check: compared the story's "Files changed" list against a filesystem scan of everything modified more recently than the last Sprint-2 verification file. Outside `lib/ingestion/**`, `test/helpers/pglite.ts`, `package.json` and `pnpm-lock.yaml`, the only touched files are process/kit files (`dev_minions/**`, `.claude/**`) from other stories/sprint activity, not application code. No scope creep in the implementation.

### Acceptance criteria

- **AC1 — Happy path, offline**: MET. `ingestEtf`/`ingestReport` (`lib/ingestion/ingest-etf.ts:45-160`) chain discover → download → text → extract → validate → select → persist in that order. `IE-1` (`ingest-etf.test.ts:107-140`) mocks `fetch` to serve `BTBETRETF-instrument-2026-09-23.html` then `BTBETRETF-2026-09-22.pdf`, asserts `saveReport` called once with `reportDate '2026-09-22'`, `sourceUrl` = the README §7 newest PDF URL, `fetchedAt instanceof Date`, `status 'ok'`, `errorMessage null`, exactly 2 values whose `numericValue`/`rawValue` are read from `expected.json` (not hard-coded), outcome `{code:'ok', reportDate:'2026-09-22', valuesWritten:2, sourceUrl}`, and `fetchImpl` called exactly twice in page-then-PDF order. `E2E-1` (`ingest-etf.pglite.test.ts:66-106`) proves the same through the real Drizzle store executed on PGlite, reading the actual `reports`/`report_values` rows back.
- **AC2 — report_date is the PDF footer date only**: MET. `IE-2` (`ingest-etf.test.ts:143-169`) fakes only `Date` (`vi.useFakeTimers({toFake:['Date']})`), sets the clock to 2031, and stubs `discover` to return a decoy `publishedAt`/title with a different date; the stored `reportDate` is still `'2026-09-22'` (from the PDF) and `fetchedAt` equals the faked clock (so the clock only feeds `fetched_at`, per the story). `BD-3` (`ingest-etf.test.ts:171-181`) source-scans every non-test file in `lib/ingestion/` for `new Date(`, `Date.now(` or `publishedAt` and asserts none is present — independently confirmed by reading `ingest-etf.ts`, `store.ts`, `select-values.ts`, `default-deps.ts` myself: none reference the clock or `publishedAt`; `reportDate` only ever comes from `result.reportDate` (`ingest-etf.ts:114,126,138,152`).
- **AC3 — tracked fields only, via one isolated function**: MET. `selectValuesToPersist` (`lib/ingestion/select-values.ts:14-37`) is pure, takes only types as imports, returns tracked-only values in order, flags a tracked key missing from `values`/unknown to the adapter as incomplete, dedupes, and gives `{complete:true, values:[]}` for zero tracked keys — all covered by `select-values.test.ts` (7 unit tests, including a no-mutation test). `ingest-etf.ts` does no field filtering of its own and imports `./select-values` (`ingest-etf.ts:5`, confirmed by `BD-2`, `ingest-etf.test.ts:225-228`). `IE-3a/3b/3c` exercise the pipeline-level behaviour: 2-of-8 tracked writes only those 2; a tracked key missing from the extraction result gives `stage:'select'` and neither `saveReport` nor `findReport` is called; zero tracked fields gives `ok`/`valuesWritten:0`.
- **AC4 — atomic write, pattern (a)**: MET. `buildSaveReportStatements` (`store.ts:61-98`) builds exactly one claim-insert (interim `'parse_error'`, guarded `where "reports"."status" <> 'ok'`), one `DELETE ... report_values WHERE report_id IN (SELECT ... status <> 'ok')`, one `INSERT ... SELECT ... FROM reports ... WHERE ... status <> 'ok'` per value (subquery-resolved `report_id`, never an earlier independent call), and a final `UPDATE ... SET status = $1 ... WHERE status <> 'ok' RETURNING id` — status is set to `'ok'` only in that last statement. `createDrizzleReportStore.saveReport` (`store.ts:111-120`) passes the whole list through one `run()` call. `SQ` (`store.test.ts`) asserts the statement shapes and params via `.toSQL()`/`getQuery()`, including "no statement except the last has `'ok'` as a set value". `PG-4b` (`store.pglite.test.ts:29-45`) proves the atomic property directly: a mid-batch numeric-cast failure inside a real `pg.transaction` leaves **zero** rows in both `reports` and `report_values` — stronger than the story's bar ("cannot leave an ok report without its values"). `PG-4c` (`store.pglite.test.ts:47-62`) proves the ordering property through a deliberately non-atomic runner: the same failure leaves a `reports` row at `status:'parse_error'`, never `'ok'`.
- **AC5 — re-runs**: MET. `IE-5a` (existing `ok` row → `already_ingested`, `saveReport` not called), `IE-5b` (existing non-`ok` row → replaced, `status:'ok'` with the new values), `IE-5c` (two `ingestEtf` calls on the same fixtures with a stateful fake store → `ok` then `already_ingested`, `saveReport` called once, and the second call still makes its own discovery+download request, `fetchImpl` called 4 times total — matches the sprint-review note this must not short-circuit), `IE-5d` (a `saveReport` race returning `already_ok` still surfaces as `already_ingested`). `PG-5a`/`PG-5b` prove the same at the SQL level on PGlite: an existing `ok` row is untouched and its original value is intact after a second `saveReport`; an existing `parse_error` row is fully replaced (old extra field gone). `E2E-2` proves it through the full pipeline against the real store: one `reports` row, `ok` then `already_ingested`.
- **AC6 — one attempt, never throws**: MET. Step order in `ingestEtf`/`ingestReport` guarantees adapter resolution before any I/O, and a `try/catch` per function turns every throw into `{code:'failed', stage, message}` (`ingest-etf.ts:45-77`, `81-160`). Tests: `IE-6b-i` (rejected `fetch` → `stage:'discovery'`, `kind:'network'`), `IE-6b-ii` (adapter `extract()` throws → `stage:'extract'` with the thrown message, not an exception), `IE-6b-iii` (`findReport` throws → `stage:'persist'`), `IE-6c` (`registry.get` throws → `stage:'adapter'`), and "a call with no adapter makes zero fetch calls" (explicit `fetchImpl` not-called assertion). `IE-1`/`IE-5c` independently confirm exactly one discovery + one download per successful call.
- **AC7 — every failure path writes nothing, names its stage/message**: MET. A parameterised table (`ingest-etf.test.ts:359-477`) covers discovery `error`(503→`http_error`)/`not_found`(`list_not_found`), download `not_pdf` and 404 (`http_error`), unreadable text, no-adapter (`null` and unknown key), adapter `ok:false`, and validation violations (an adapter returning an unknown field key). Every case asserts `code:'failed'`, the expected `stage`/`kind`, a non-empty `message`, **and** `store.saveReportCalls`/`store.findReportCalls` both `toHaveLength(0)` — stronger than the story's "store's write method is never called" since `findReport` is checked too, matching the plan's stated reasoning (the date isn't known yet on these paths).
- **AC8 — boundaries**: MET. `boundaries.test.ts` scans every non-test `.ts` file under `lib/ingestion/` (asserts `>= 4` files found, so the check can't pass vacuously) for `next`/`react`/app/component imports, AI-related specifiers, and `process.env`; additionally forbids `unpdf`/`@neondatabase/serverless`/`../db/index`/`../db` specifically in `ingest-etf.ts` and `select-values.ts`. A positive-control test proves the extractor itself catches all four specifier forms. `default-deps.test.ts` (`IE-8`) verifies `createDefaultIngestDeps()` wires the real Sprint-2 functions plus a working store using `vi.mock("@neondatabase/serverless")`/`vi.stubEnv`, without invoking any method (no network). Every IE/E2E test stubs global `fetch` to throw in `beforeEach` and relies on real assertions of call counts, so "no live network" is non-vacuous (the Sprint-2-audit pattern). `store.ts` imports `Db` from `../db/index` only as a type (`store.ts:2`, `import type`), so it never calls `getDb()` itself, consistent with the plan; the boundary test correctly doesn't restrict `store.ts`'s specifier list since only `ingest-etf.ts`/`select-values.ts` are checked for those imports, matching the plan's stated design (`store.ts` is DB-adjacent by necessity, `default-deps.ts` is the only file that actually reaches the network/DB).
- **AC9 — gates**: MET. `pnpm typecheck` — clean. `pnpm lint` — 0 errors, 1 pre-existing unrelated warning. `pnpm test`/`pnpm build`/clean `--frozen-lockfile` install not run by me (out of my permitted tool scope); `story-tester`'s independent run is the check of record for those. `package.json`/`pnpm-lock.yaml` correctly add `@electric-sql/pglite@0.5.8` as an exact-pinned devDependency (confirmed present and version-locked in `pnpm-lock.yaml`) and tighten `unpdf` from `^0.11.0` to `0.11.0` (Sprint 2 audit N1, confirmed in both files). `pnpm-workspace.yaml` is untouched — correctly so, since PGlite needs no build-approval prompt (confirmed no placeholder text, unlike the US-008 round-1 Critical).

### Non-negotiable rules (AGENTS.md)

- Deterministic label-based extraction, no AI: unaffected by this story (`lib/extraction/**` untouched, confirmed via `BD-3`'s prose and my own read of `ingest-etf.ts`/`select-values.ts`/`store.ts`/`default-deps.ts` — no adapter logic added here, only orchestration/persistence). `boundaries.test.ts` also positively excludes AI-related specifiers.
- Adapter per report format: unchanged, `registry.get` used exact-match, no fallback added.
- Empty day on missing report: out of scope for this story (US-014 decides which failures get a `reports` row); AC7 confirms failure paths write nothing, consistent with "never guess a value" and not inventing a row prematurely.
- next-intl ro+en: N/A — no UI/strings in this story (confirmed no `next`/`react`/component imports via `boundaries.test.ts`).
- DEC-007 number display: N/A — no UI/number formatting in this story; `numericValue`/`rawValue` are stored verbatim per the existing contract.
- No secrets in code/logs: confirmed no `process.env` in `lib/ingestion/` (only `default-deps.ts`'s `getDb()` reaches it, indirectly, inside `lib/db/index.ts`, pre-existing and outside this story's boundary); no secret literals found in the changed files.
- No weakened/skipped tests: none found; new tests are additive, and the two "US-012 interim: ..." tests are explicitly named as forward-compatible (US-014 will supersede them), which is disclosure, not weakening.
- No scope creep: confirmed above — files changed match the declared list exactly; the extra `sourceUrl` field on the `ok` outcome and the two extra "never downgrade ok" SQL guards beyond the story's literal ask are both additive and directly serve AC4/AC7, not speculative.
- Git: I ran no git command, not even read-only.

### Findings

None Critical. None Warning.

**Notes** (non-blocking):
1. `ingest-etf.ts:132-134` and `:145-147` both return the same `already_ingested` shape from two different call sites (the `findReport`-known-`ok` short-circuit, and the `saveReport`-race short-circuit). This is intentional per the plan (IE-5a and IE-5d), not a defect — flagging only because a future refactor could accidentally diverge the two shapes; a shared helper would make that impossible rather than merely tested-against.
2. The `already_ingested` outcome type carries no `sourceUrl`, while `ok` does — an asymmetry the story doesn't require either way, but worth the PO/tech-lead noting if a future story (US-013's run summary) wants the source URL for an `already_ingested` row too.
3. `buildSaveReportStatements`'s `clearOldValues` statement runs unconditionally (even on a brand-new report with no prior values to clear) — harmless (deletes zero rows) and cheaper than adding a conditional, not a defect.

### Scope deviations

None found beyond the two additive, non-blocking items already noted above (extra `sourceUrl` on the `ok` outcome; the SQL guard doing slightly more than the letter of AC4 by proving the stronger "zero rows on atomic failure" property, not just "no ok-without-values"). Neither changes behaviour the story or its ACs constrain, and both are net-positive for US-013/014's reuse per the plan's R8.
