# HANDOVER — live state of automated delivery
_Last updated: 2026-09-25 by Claude Code (autopilot via /goal — US-018 delivered, picking US-019 next)_
Automation state: RUNNING

Read this first, whatever agent you are (Claude Code, GitHub Copilot). Rules: AGENTS.md. Agents never run git — not even read-only; the user does.

## Active story
- Story: US-019 — ETF detail page: time-series charts for tracked fields
- Phase: plan
- Round: 0

## Acceptance criteria (active story)
- See `dev_minions/backlog/stories/US-019.md`.

## Files changed (active story)
- (none yet)

## Failing / open
- — (US-018 handed off clean: typecheck, lint, 744/744 tests, build with `DATABASE_URL` unset all pass, `ƒ /etf/[symbol]` confirmed dynamic)

## Exact next step
- US-018 delivered round 1: both `story-reviewer` and `story-tester` PASS, no fix loop needed. QA checklist written (`US-018-qa.md`), status.md updated to `Awaiting QA`. Reviewer noted 3 non-blocking test-hygiene notes only (a test-naming mismatch in `page.test.tsx`, a minor asymmetry in `EtfDetail.test.tsx`'s state assertions, and that the reviewer's own tool scope only re-ran typecheck/lint, not test/build — the tester independently re-ran and confirmed all four gates).
- Now picking US-019 (depends on US-018, which is Awaiting QA — eligible). Sprint 4 tech-lead review already settled "one chart per field" for this story (see the 2026-09-25 sprint-04 detailing log entry below). Read `dev_minions/backlog/stories/US-019.md` and its "Suggested model/thinking" line to decide whether to delegate planning to `story-planner`; it likely introduces a new charting dependency (Recharts, per ADR-001) so check whether that needs a decision note.
- Note: US-016 was implemented/reviewed by GitHub Copilot in a prior session (Copilot fallback); this Claude Code session resumed the dev-loop autopilot from there and has since delivered US-017 and US-018 on top of it. **Sprint 4 (US-016..US-019) will be fully Awaiting QA/Done once US-019 lands — run `tech-lead` "sprint-audit 4" next, before picking a Sprint 5 story (roadmap.md not yet checked for a title).**

## Note: unpdf version pin (R1, plan-anticipated)
- `pnpm add unpdf` installed the latest, `1.8.1`. Its `extractText({ mergePages: true })` inserts `\n` between text items — the spike's `0.11.0` (validated in `spikes/pdf-extraction/FINDINGS.md`) does not. AC2's no-`\n` guard (US-010's flattened-text contract) caught this immediately, exactly as the plan's R1 anticipated. Per the plan ("pin the newest version that passes AC2 unchanged... do not adapt the text to hide it"), pinned to `unpdf@0.11.0` — text output now matches the spike again, all 143 tests pass. This is a version choice inside ADR-001's existing library choice, not a new DEC. Side note: `1.8.1`'s `PDFDocumentProxy` type has no `destroy()` (only `cleanup()`); `0.11.0` has both, so the code uses `destroy()` as the plan specified. `pnpm add unpdf@0.11.0` printed `ERR_PNPM_IGNORED_BUILDS` for `canvas`'s postinstall script (a pdf.js peer dep, used only for page-to-image rendering, which this story never calls) — the package still installed correctly (verified via `node_modules/unpdf/package.json` and the full green test/build run); left unapproved since we don't need canvas's native build.


## Waiting on the user
- QA: US-008 — checklist at `dev_minions/verification/US-008-qa.md`. Round 1 tests PASS, round 1 review FAIL (Critical: `pnpm-workspace.yaml`'s `allowBuilds.canvas` left as unresolved placeholder text broke a clean install), fixed and re-verified; round 2 review PASS, round 2 tests PASS (143/143 incl. 39 new). No manual checks of its own — live PDF download/extract deferred to US-011 AC7. Non-blocking note: `unpdf` pinned to `0.11.0`, not latest (`1.8.1` breaks the flattened-text contract, see the version-pin note above).
- QA: US-009 — checklist at `dev_minions/verification/US-009-qa.md`. Round 1 review PASS, round 1 tests PASS (235/235 incl. 92 new), no fix loop needed. No manual checks — pure TypeScript, no DB/network/UI. Non-blocking note: one cosmetic ESLint warning (unused `_text` param in a test, no `argsIgnorePattern` configured), lint still exits 0.
- QA: US-010 — checklist at `dev_minions/verification/US-010-qa.md`. Round 1 review PASS, round 1 tests PASS (315/315 incl. 80 new), no fix loop needed. No manual checks — pure text-in/struct-out; the implementer's smoke check against the three real PDF fixtures already showed all 8 fields found with zero violations. Non-blocking notes: investors label searched from position 0 rather than units.end (harmless, guarded elsewhere); `canHandle` duplicates one label literal instead of deriving it from `LABELS.units`.
- Environment: `scripts/claude/autopilot.sh` shows as an unreadable/permission-broken file on disk (`ls -la` shows `-?????????`), found during the Sprint 2 audit. No agent tool call in any log touched it (checked `dev_minions/automation/logs/`); likely a WSL1/DrvFs filesystem quirk (same class as DEC-001/DEC-008), not an agent action. The user should check it and reinstall with `bash scripts/claude/install-kit.sh` before the next unattended `tmux ... autopilot.sh` run.
- Process: Sprint 2 audit (`SPRINT-02-audit.md`) found **3 denied git-command attempts** this sprint (2 by `story-reviewer`, 1 by the main session during US-008) — all correctly blocked by the permission system, no state changed, but `US-009-review.md` states "no git commands were run" which is misleading since one was attempted and denied. This is the second sprint this has happened (see US-002's reviewer note in the Log). The audit recommends a DEC requiring verdict files to disclose denied git/`.env*` attempts, not just successful ones. Flagging for the user/Technical Lead chat to decide; not blocking, no story affected.
- Decisions: —
- Live steps: US-006's Neon/Vercel setup, migration, seed, deployment and `/health` verification were accepted by the user on 2026-09-24. US-007/US-011's live BVB discovery-and-extraction checks were accepted by the user on 2026-09-24.
- Escalations: —
- Kit: DEC-013's kit update (retires `qa-runner`, updates `deliver-story`/`tech-lead` to stop at `Awaiting QA`) is in `pending-kit/`, not yet installed — run `bash scripts/claude/install-kit.sh` before the next dev-loop cycle.
- Process: the Codex QA/Deploy loop (DEC-013) is designed and already active informally (see the Log below) but not yet following the new write scope — paste `dev_minions/automation/qa-goal.txt` into that Codex session so it writes to `verification/US-XXX-qa-run.md` and its own `## QA/Deploy log (Codex)` section at the bottom of this file, not this shared Log. It has no git access — it will flag stories as ready to ship in that log section, and you push them yourself whenever you choose.

## Log (newest first, one line each)
- 2026-09-25 — Sprint 4 (US-016..US-019, monitoring UI) detailed by `story-planner`, approved by `tech-lead` "sprint-review 4" (`SPRINT-04-review.md`) — APPROVED, no story blocked. Tech-lead settled 3 technical decisions in place (US-016 columns = union of tracked fields across active ETFs, not intersection; `timeZone: "Europe/Bucharest"`; US-019 one chart per field) and fixed plan gaps (US-016 must replace the old DEC-007-violating `lib/format.ts` `formatNumber`; US-017's previous-day lookup can't be "the same batch" as written, now SQL `report_date - 1` or a second read, with a boundary test either way; US-017's sign follows the rounded value so `-0,00%` can't appear; US-019's chart needs a dot on every point, and AC2's parse_error exclusion moves to a PGlite read-model test). All 9 product questions stay NEEDS USER for the demo; each story ships an isolated default meanwhile. Added US-016 (Ready), US-017/018/019 (Blocked on deps) to status.md. Picking US-016 next.
- 2026-09-24 — US-012 delivered. Implemented `lib/ingestion/select-values.ts` (pure tracked-field selection, US-014/PRODUCT-1 extension point), `store.ts` (DEC-010 pattern (a): one `db.batch` per report+values, `report_id` resolved by a subquery on `(etf_id, report_date)` inside the same batch, every statement guarded by `status <> 'ok'`, the row flipped to `ok` only in the final `RETURNING id` statement — so a concurrent already-`ok` write is never downgraded, proven in SQL not just by the caller), `ingest-etf.ts` (`ingestEtf`/`ingestReport`, Task-1 step order: adapter → discover → download → text → extract → validate → select → persist, one `try/catch`, never throws), and `default-deps.ts` (the only file wiring concrete I/O — Sprint 2's discovery/pdf/registry functions plus `getDb()`, called lazily). Added `@electric-sql/pglite@0.5.8` as an exactly-pinned devDependency (plan R1, story-authorised) to execute the batch/guard SQL offline against the real shipped statements (`test/helpers/pglite.ts` applies `drizzle/0000_init.sql` in memory) — proved atomic-failure rollback, the non-atomic ordering property, and the race guard (an existing `ok` row surviving a later `saveReport` attempt) that `.toSQL()` alone couldn't reach. Also pinned `unpdf` `^0.11.0` → `0.11.0` exactly (Sprint 2 audit N1). 60 new tests across 7 files (443/443 total, including 2 PGlite-backed E2E tests running the full pipeline). Ran the AC9 clean-install proof (`rm -rf node_modules && pnpm install --frozen-lockfile`) after the `package.json` change — exit 0, no `pnpm-workspace.yaml` prompt needed (PGlite has no install scripts). Round 1: review PASS, tests PASS — no fix loop. No Critical or Warning findings; three Notes, all non-blocking (duplicated `already_ingested` return shape at two call sites, intentional; `already_ingested` has no `sourceUrl` unlike `ok`, a possible future want; `clearOldValues` runs unconditionally even for a brand-new report, harmless). Status: Awaiting QA (`US-012-qa.md`) — nothing MANUAL-QA-specific to this story; the live Neon write is proven by US-013. US-013 and US-014 both now eligible (US-015 still waits on both); picking US-013 next.
- 2026-09-24 — Technical Lead shipped DEC-013: split delivery into two independent loops to cut Claude usage. This dev loop (Claude Code) is unchanged except QA comes out of it — it stops at `Awaiting QA` and no longer waits for a verdict. The Codex loop below had already started informally, writing straight into this shared Log; it now has a proper brief (`dev_minions/roles/qa.md`) and its own write scope: a story's `verification/US-XXX-qa-run.md`, that story's Story board row in `status.md`, and a new `## QA/Deploy log (Codex)` section at the very bottom of this file — **not** this shared Log, to avoid exactly the kind of collision the entry below is an example of. It has no git access at all — "no agent runs git" stays absolute with zero exceptions — so instead it logs a one-line notice whenever a story passes QA, naming it as ready for the user to push whenever they choose. A FAIL there reopens the story (`Ready — reopened by QA`) for this loop to fix; Codex never edits code itself. Kit update in `pending-kit/` retires `qa-runner` (never installed, never ran) and updates `deliver-story`/`tech-lead` accordingly — needs `bash scripts/claude/install-kit.sh` before the next dev-loop cycle. See `decisions/DEC-013-split-autopilots.md`.
- 2026-09-24 — User accepted US-007 and US-011 after the live BVB checks and Codex's independent QA re-check. Recorded both Done in `status.md`; US-008 through US-010 remain Awaiting QA.
- 2026-09-24 — Independent QA re-check for US-007/US-011 by Codex: `pnpm typecheck` passed; lint had only the known non-blocking unused `_text` test-parameter warning; `pnpm test` passed 25 files / 387 tests; `pnpm build` passed. Live read-only `pnpm report:latest` runs for BTBETRETF, TVBETETF and PTENGETF each returned a 2026-09-23 report, all eight fields and a BVB PDF URL; units and investor sub-totals equal their reported totals in all three outputs. Independent BVB page reads showed each matching 24.09.2026 top-news filing time (09:25, 09:08, 09:09 respectively). Recommendation: PASS. User acceptance of US-007 and US-011 remains required before changing their status.
- 2026-09-24 — User completed US-004's local RO/EN switch-and-persistence check and accepted the agent-written Romanian copy. Recorded US-004 Done in `status.md`; remaining QA stories are unchanged.
- 2026-09-24 — User completed US-005's live first/second seed checks (second run idempotent; expected 3/8/6/1 row counts confirmed), approved the Romanian/English catalogue labels, and accepted US-005. Recorded Done in `status.md`; remaining QA stories are unchanged.
- 2026-09-24 — User completed the US-006 live Neon/Vercel setup, migration, seed and health-check QA, and accepted US-006. Recorded Done in `status.md`; remaining QA stories are unchanged.
- 2026-09-24 — Sprint 3 detailed by `story-planner` (4 stories, US-012..US-015, automation & persistence) and approved by `tech-lead` "sprint-review 3" — APPROVED, no story blocked. Tech-lead fixed several gaps in place: US-012 gained US-011 as a dependency (AC1 needs its 2026-09-22 fixtures), renamed success code `ingested`→`ok` for consistency across US-013/014/015, and got a Notes warning about `db.batch()` driver support; US-013 now records a throwing `ingestEtf` as `internal_error` (so US-015 counts it) and orders tracked fields by `display_order`/`field_key`; US-014 got an appended review section requiring its plan to list which US-012 tests it deliberately replaces and to enforce "never downgrade an `ok` row" inside the DB write itself, not just via `findReport`; US-015 now counts every non-`ok`/non-`already_ingested` outcome as an error and specifies the `failStaleRuns`-failure path. Three technical decisions settled in place (cron `0 10 * * *` UTC; no-date failures get no `reports` row; partial extraction keeps found values under `parse_error`). Two product questions (track-only vs. every field; catch-up filings ingesting only the newest of a multi-day filing) go to the demo, non-blocking — the planner's recommendation is noted but not applied without the PO. Added US-012 (Ready) and US-013/014/015 (Blocked on dependencies) to status.md Story board. Picking US-012 next.
- 2026-09-24 — Sprint 2 audit (`tech-lead`, "sprint-audit 2"): verdict PASS, no Critical findings, no story reopened. Independently re-verified US-007's fixture-URL table by hand and US-011's manifest with pdf-parse on three PDFs — both matched exactly. Four Warnings, none making a criterion false today (see `SPRINT-02-audit.md` and this file's "Waiting on the user" section above for the two worth carrying forward: the denied-git-attempts disclosure gap, and the `scripts/claude/autopilot.sh` filesystem anomaly). Five Notes, all non-blocking (unpdf not exactly pinned; discovery's row regex is brittle to markup changes; two adapter sub-searches unbounded at the end, harmless on all 6 fixtures; `--save` intentionally still writes on partial extraction; data-model.md's separator note already fixed). Moving to Sprint 3.
- 2026-09-24 — US-011 delivered. Cross-checked the plan's by-eye transcriptions for all three committed fixtures against `spikes/pdf-extraction/compare.mjs`'s independent pdf-parse output before writing `expected.json` — exact match, no adjustment needed. Implemented `lib/extraction/fixtures.test.ts` (manifest runtime-shape validation, `diffFixtureSets`/`sumsExactly` pure helpers with their own synthetic tests, a `beforeAll`-cached PDF→text pipeline run per fixture, AC1/AC2/AC4/AC5 assertions) and `lib/extraction/report-latest.ts` (`runReportLatest` with fully injected deps per the plan's sketch, `defaultWriteFileExclusive` using `wx` for atomic no-overwrite writes, R6's partial-extraction rule: print MISSING fields, exit 1, but still save with `--save` since a partially-readable report is exactly the fixture worth capturing) plus its thin entry point `scripts/report-latest.ts` and `package.json`'s `report:latest` script. 72 new tests (387/387 total). Ran the AC3 temporary-change proof by hand (copy an existing PDF with no manifest entry → fails naming it; add a manifest entry with no PDF → fails naming it; revert → green), appended the observed failures to the plan. Ran the AC8 offline entry-point smoke check (`pnpm report:latest` and `pnpm report:latest NOPE`, both exit 1 with clear messages) and confirmed `pnpm typecheck && pnpm lint && pnpm test && pnpm build` all green. Task 5: bvb.ro was reachable from this session, so captured one new fixture per ETF via `pnpm report:latest <SYMBOL> --save` (report date 2026-09-22, different from the committed 2026-09-21 fixtures), cross-checked all three against the spike's pdf-parse output before adding their manifest entries — manifest now has 6 fixtures. Round 1: review PASS, tests PASS — no fix loop. Reviewer independently re-ran the spike's compare script and hand-verified the three new entries itself (satisfying the story's "check a non-BTBETRETF entry by hand" note). Two Note-level findings, both non-blocking (the tester's verdict file has stale prose about fixture count, though its numbers are correct; the CLI's plain-text output is intentional, not a DEC-007 gap). Status: Awaiting QA. **Sprint 2 (US-007..US-011) is now fully Awaiting QA** — running `tech-lead` "sprint-audit 2" next, before picking a Sprint 3 story.
- 2026-09-23 — US-010 delivered. Implemented `lib/extraction/adapters/numbers.ts` (`REPORT_NUMBER_PATTERN`, comma-thousands/dot-decimal, no sign, rejects `ro`-format) and `brd-depositary.ts` (label-anchored extraction for all 8 catalogue fields, report date from the footer phrase only — never the filing stamp, FINDINGS trap 2 — with conflicting-footer-dates giving `ok: false`; `nav_per_unit` from the documented positional VUAN gap, FINDINGS trap 1, accepted only when the gap is exactly one parseable token; units/investors `Persoane fizice`/`Persoane juridice` pairs told apart by bounding the units sub-search to end at the investors label, fixing the ambiguity the tech-lead's sprint review caught at detail time). Registered in `default-registry.ts`. 80 new tests (315/315 total) built around a `buildBrdText`/`run`/`valueOf` scaffold per the plan, including anti-vacuity guards on every label-removal case and an A-B-A purity test. Hit one test-helper bug during implementation (`valueOf` returned the full `ExtractedValue` including `fieldKey`, failing 13 `toEqual` comparisons against `{numericValue, rawValue}` — fixed by stripping `fieldKey` in the helper, not a product bug). Ran the plan's optional (non-committed) smoke check against all three real PDF fixtures before handover: all three gave the correct date, all 8 values, zero violations — no rule needed adjusting against real text. Round 1: review PASS, tests PASS — no fix loop. Two Note-level findings, both non-blocking (investors-label search not bounded from units.end as the design sketch suggested, harmless; one label literal duplicated in `canHandle`). Status: Awaiting QA. US-011 now fully eligible (US-007/US-008/US-010 all Awaiting QA) — Sprint 2's last story; picking it next.
- 2026-09-23 — US-009 delivered. Implemented `lib/extraction/adapters/{types,validate,registry,default-registry}.ts` per `story-planner`'s plan: `Map`-based registry with exact-match `get` (no trim/case-fold, guards against `"constructor"`/`"__proto__"` prototype-key lookups), non-short-circuiting `detect` (calls every adapter's `canHandle`, so two claimants correctly resolve to `undefined` instead of picking the first), `validateExtractionResult` with 7 violation rules including two hardening ones beyond the story (`empty_raw_value`, `empty_error`), and pure-arithmetic `isIsoCalendarDate` (no `Date`, avoids engine-dependent rollover/timezone behavior). `REGISTERED_ADAPTERS` is an empty list — US-010 adds `brd-depositary`. AC1's compile-time proof (`@ts-expect-error` + `expectTypeOf`) is carried by `pnpm typecheck`. 92 new tests (235/235 total), typecheck/lint/build all green. Round 1: review PASS, tests PASS — no fix loop. No findings beyond one cosmetic lint warning. Status: Awaiting QA. US-010 now eligible (US-009 Awaiting QA); picking it next.
- 2026-09-23 — US-008 delivered. `pnpm add unpdf` installed latest `1.8.1`; AC2's no-`\n` guard immediately caught that its `extractText` now inserts line breaks between text items (US-010's flattened-text contract), exactly the version-drift risk the plan's R1 flagged — pinned to `unpdf@0.11.0` (the spike's validated version) per the plan's own instruction to pin rather than adapt the text, all 143 tests green after. Implemented `lib/extraction/pdf.ts` (`downloadReportPdf`, `extractPdfText`, `hasPdfSignature` — 1024-byte signature window, `%PDF-` never trusts `Content-Type`), 39 new tests. Round 1: tests PASS, review **FAIL** — Critical finding: `pnpm add`'s interactive build-approval prompt left `pnpm-workspace.yaml`'s `allowBuilds.canvas` as literal unresolved text (`set this to true or false`, from `canvas`, a transitive dep of unpdf/pdf.js used only for page-to-image rendering this story never calls), which made a genuinely clean `pnpm install --frozen-lockfile` hard-fail with `ERR_PNPM_IGNORED_BUILDS` even though the story's own local checks passed against an already-populated `node_modules` — the reviewer caught it by running `rm -rf node_modules && pnpm install --frozen-lockfile`. Fixed: `canvas: false`; verified the same clean-install command now exits 0. Also fixed the round's one Warning (5 `downloadReportPdf` test cases missing `toHaveBeenCalledTimes(1)`). Round 2: review PASS, tests PASS. Status: Awaiting QA. US-010 stays Blocked on US-009, which is now eligible (US-005 Awaiting QA); picking US-009 next.
- 2026-09-23 — US-007 delivered. Phase A (live investigation) confirmed Branch A: report links sit directly in the GET response (`<table id="gv5News">`, no `__doPostBack`), the honest User-Agent got no 403 (C3 didn't trigger), and no headless browser is needed (C2 didn't trigger) — none of the plan's contingent DECs fired, so no decision was needed. Re-captured all three fixtures fresh via live `fetch` with the documented headers (status 200/200/200, no redirect, utf-8) and wrote `test/fixtures/bvb/README.md` documenting the request, the `gv5News` list container, the "VAN la data …" identifying rule (folded-text match), the `D.MM.YYYY H:mm:ss` filing-timestamp format, and the expected newest URL per fixture (read by hand). One real-page wrinkle not anticipated by the plan: a "catch-up" row can hold several PDF links sharing one title/timestamp (a multi-day gap filed at once) — resolved as an implementation judgment call, not a DEC, since it's a parsing-tiebreak detail, not architecture: documented in the README as an observed BVB convention (links listed oldest→newest) and implemented as "same-row ties, later link wins", covered by a dedicated test. Implemented `lib/extraction/http.ts` (`fetchOnce`, shared single-attempt fetch/timeout/classify, reusable by US-008), `lib/extraction/html.ts` (entity decode incl. Romanian ș/ț which have no NFD decomposition — handled explicitly, tag strip, whitespace collapse, fold-for-match), `lib/extraction/discovery.ts` (`parseReportList`, `isDepositaryReportEntry`, `findLatestReportLink`, `discoverLatestReport`). 48 new tests (29 discovery, 11 http, 8 html), all green with existing suite (104/104), typecheck/lint/build all green. Round 1 review PASS, round 1 tests PASS — no fix loop. Reviewer notes (all Note-level, non-blocking): README byte-size table off by +9/file (UTF-16 `.length` vs actual UTF-8 bytes, cosmetic, content itself correct); `discoverLatestReport` parses HTML twice internally (harmless duplication); `list_not_found` branch not exercised through a full mocked-fetch round trip (low risk, only unit-tested via `parseReportList` directly). Status: Awaiting QA. US-009 already eligible (deps satisfied by US-005 Awaiting QA); picking US-008 next.
- 2026-09-23 — Sprint 2 detailed by `story-planner` (5 stories, US-007..US-011, extraction core) and approved by `tech-lead` "sprint-review 2" — APPROVED, no story needs a user decision. Tech-lead fixed two gaps in place before approving: US-010's units sub-field search had no end boundary (could silently borrow the investors block's value) and its AC7 contradicted the label-removal rules; US-011's AC6 failure list didn't match its own Task 4. Independent spot-check: tech-lead ran `unpdf` on all three committed fixtures itself and confirmed VUAN/labels/sums. Non-blocking note: `data-model.md:85` still says thousands separator `.`, contradicts the US-001 spike's measured `,` — needs a PO/Technical-Lead doc fix, not a story. Added US-007..US-011 to status.md Story board (US-007/008/009 Ready, US-010/011 Blocked on deps).
- 2026-09-23 — US-005 (seed script) and US-006 (`/health` page + deploy docs) delivered, both round 1 PASS/PASS, no fix loop; both planned directly (simple, ≤6 ACs, no schema/adapter/AI/cron/auth touch). Sprint 1 now fully delivered (US-001..US-006 Done or Awaiting QA) — ran `tech-lead` "sprint-audit 1": verdict PASS, no Critical findings, no story reopened. Six Warnings logged (see US-005-qa.md, US-006-qa.md and `verification/SPRINT-01-audit.md`); notably W3 corrected an earlier US-006 reviewer Warning that was factually wrong (next-intl's sync hooks throw in async Server Components, so `app/health/page.tsx`'s use of `next-intl/server` is correct, not a deviation) — withdrawn from US-006-qa.md. Moving to Sprint 2.
- 2026-09-23 — US-004 delivered: resumed mid-implement from the prior session's handover (code and tests already written); ran local checks, all green (typecheck, lint, 45/45 tests, build). Fixed a test-only bug in `components/LanguageSwitcher.test.tsx` (a fixed-width string slice around the RO button's index went negative and silently produced an empty string, masking the assertion; replaced with a per-button regex match). Round 1 review PASS, round 1 tests PASS — no fix loop. Reviewer flagged one non-blocking Warning: `i18n/request.ts` has no `timeZone` configured (harmless now, no dates formatted yet; revisit before any story that formats dates/times). Status: Awaiting QA.
- 2026-09-23 — Cycle 1 hit the 5-hour usage limit at 16:21 mid-US-004 (implement, local checks not run); cycle 2 hit it immediately. The runner detected it and is waiting. Technical Lead rebuilt US-004's Files changed from the log and shipped DEC-011: PostToolUse hook logging every file write to dev_minions/.files-touched.log, runner sleeps until the exact limit reset (waits no longer count as cycles), .gitignore for logs/runtime files.
- 2026-09-23 — US-003 delivered: story-planner planned it (complex, DB schema); two technical clarifications (FK nullability, Neon driver mode) resolved in DEC-010 by the in-loop tech-lead (Decided), which also updated data-model.md's FK rows to NOT NULL and left a binding note for Sprint 3 ingestion (US-012 must not split the report+values write across two independent HTTP-driver calls). Implemented `lib/db/schema.ts` (7 tables), `lib/db/index.ts` (lazy client, MissingDatabaseUrlError), generated `drizzle/0000_init.sql`, wrote `schema.test.ts`/`index.test.ts` (19 tests). Round 1 review PASS, round 1 tests PASS — no fix loop. One optional offline smoke check (drizzle-kit migrate driver resolution against a literal localhost URL) was denied by the sandbox for the implementer but succeeded for story-tester in their own sandbox; not a blocking AC either way. Status: Awaiting QA. US-005 and US-006 now eligible (deps satisfied by "Awaiting QA").
- 2026-09-23 — Technical Lead installed the autopilot kit (DEC-009): new `tech-lead` subagent; `story-planner` can detail sprints; deliver-story runs continuously across sprints and stops only when nothing is eligible without the user (demo file); `scripts/claude/autopilot.sh` cycles Claude Code with fresh context; `Automation state:` line added here. Reviewer/tester briefs now forbid read-only git explicitly (after US-002's reviewer ran `git status/log/diff`).
- 2026-09-23 — User confirmed QA + `git commit` done for both US-001 and US-002. Marked both Done in status.md (Story board + Done section); US-003 and US-004 moved from Blocked to Ready.
- 2026-09-23 — US-002 delivered: both story-reviewer and story-tester PASS round 1. Scaffolded Next.js 16 App Router + TS + Tailwind + Vitest + pnpm; all 7 ACs green (install/dev/test/lint/build, folder structure, .env.example, README). Two environment fixes documented in new DEC-008: Turbopack's persistent cache crashes on this WSL1/DrvFs mount (fixed via `--webpack` in dev/build scripts), and NODE_EXTRA_CA_CERTS must be exported explicitly per non-interactive WSL command. Reviewer flagged (Warning, non-blocking) a `.gitignore`/`.claude/settings.json` `.env.*` coverage gap — fixed immediately after the PASS verdict. Note: the story-reviewer subagent self-reported running 3 read-only `git status`/`git log`/`git diff` commands during review, which AGENTS.md forbids for any agent — no state was changed, flagging for awareness, no action taken. Status: Awaiting QA.
- 2026-09-23 — Technical Lead (now a persistent, file-access role per DEC-006) wrote ADR-001's Decided status and validation rationale to disk (previously only validated verbally in a prior, file-less chat); updated status.md's ADR-001 wording to Decided and cleared it from this file's "Waiting on the user". US-002 (Project scaffold) is next eligible.
- 2026-09-23 — US-001 delivered: both story-reviewer and story-tester PASS round 1 (unanimous, no fix loop needed). Findings: use unpdf, no OCR needed, comma/dot number formatting, two adapter traps documented (VUAN value precedes its label; filing-stamp date vs footer report date). Added root .gitignore (node_modules/.next/.env*) per reviewer warning about spikes/pdf-extraction/node_modules being untracked. Status: Awaiting QA.
- 2026-09-23 — Picked US-001 (only eligible story). This Windows machine has no Node.js; user directed use of WSL1 "Ubuntu" distro (node v20.20.2, pnpm 12.5.1) instead of installing Node on Windows.
- 2026-09-23 — Automation kit v2 installed (DEC-005, no git). ADR-001 accepted per user.

## QA/Deploy log (Codex)
_Owned by the separate Codex QA/Deploy loop (DEC-013) — it appends here only, newest last, and
never edits anything above this line. The dev loop (Claude Code) never writes to this section.
(The Codex QA re-check of US-007/US-011 above, in the shared Log, predates this section and
this decision — future QA activity belongs here and in `verification/US-XXX-qa-run.md` instead.)_
- (nothing yet)
- 2026-09-24 16:18 — US-008 QA PASS: 39 focused PDF tests, typecheck and production build passed; lint exits 0 with only the known unused test-parameter warning. Ready for the user to push whenever they choose.
- 2026-09-24 16:21 — US-009 QA PASS: 94 focused adapter tests, typecheck and production build passed; lint exits 0 with only the known unused test-parameter warning. Ready for the user to push whenever they choose.
- 2026-09-24 16:25 — US-010 QA PASS: 86 focused BRD-adapter/boundary tests, typecheck and production build passed; lint exits 0 with only the known unused test-parameter warning. A transient concurrent-build lock cleared on one retry. Ready for the user to push whenever they choose.
- 2026-09-24 16:29 — US-012 QA PASS: 56 focused ingestion/PGlite tests, typecheck and production build passed; lint exits 0 with only the known unused test-parameter warning. Two non-blocking product choices are logged in `US-012-qa-run.md`. Ready for the user to push whenever they choose.
- 2026-09-24 - User reports the QA-passed changes have been pushed. Deployment has not been independently smoke-checked here because no deployed project URL is recorded in the workspace.
- 2026-09-24 - User confirmed deployed health check PASS at https://etf-monitor2.vercel.app/health. Independent web-tool smoke check was unavailable because that endpoint is inaccessible to the tool.
- 2026-09-25 09:44 - US-013 QA PASS: focused cron/runner tests passed (38 checks across six suites, with one transient aggregate WSL timeout cleared by a 6/6 isolated route rerun); typecheck and production build passed; lint exited 0 with two known test-helper warnings. Local unauthenticated route failed closed with HTTP 500 when unconfigured. Ready for the user to push whenever they choose. Live Vercel/Neon checks are listed in `US-013-qa-run.md`.
- 2026-09-25 09:53 - US-014 QA PASS: focused failure-path/PGlite tests and the full regression suite completed successfully; typecheck and production build passed; lint exited 0 with two known test-helper warnings. No live or user-judgment checks remain for this story. Ready for the user to push whenever they choose.
- 2026-09-25 10:42 - US-015 QA PASS: focused cron/job-run/PGlite tests and the full regression suite completed successfully; C1's real environment-to-log/response secret-redaction test passed; typecheck and production build passed; lint exited 0 with three known test-helper warnings. Local unconfigured cron route failed closed with HTTP 500. Ready for the user to push whenever they choose. Live Vercel/Neon checks are listed in `US-015-qa-run.md`.
- 2026-09-25 - User reviewed the Vercel cron logs and accepted US-015. Its Story Board row is now Done; scheduled-run confirmation remains an observational check for the next day.
- 2026-09-25 16:15 - US-016 QA PASS: focused PGlite/formatter/page/render tests, full regression suite, typecheck, lint, and offline production build passed. Local Romanian and English home routes returned HTTP 200 with only the translated generic no-database error; server stopped cleanly. Ready for the user to push whenever they choose. Live Neon/Vercel and product-judgment checks are listed in `US-016-qa-run.md`.
- 2026-09-25 16:22 - US-017 QA PASS: 84 focused exact-delta/PGlite/render checks and the full 744-test regression suite passed; typecheck, lint, and offline production build passed. Local Romanian and English home routes returned HTTP 200 with only translated generic no-database errors; server stopped cleanly. Ready for the user to push whenever they choose. The single live delta calculation and non-blocking product/copy checks are listed in `US-017-qa-run.md`.
- 2026-09-25 16:25 - US-018 QA PASS: 53 focused history/detail/navigation checks passed; the immediately preceding independent full 744-test/typecheck/lint/build run remained green with `DATABASE_URL` unset. Local known and unknown ETF routes returned HTTP 200 with translated generic no-database errors, including EN; server stopped cleanly. Ready for the user to push whenever they choose. Live Neon/Vercel and product/copy checks are listed in `US-018-qa-run.md`.
